<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.UserMapper">
    <select id="queryUserByUserName" resultType="com.example.blog.entity.User">
        select * from user where username=#{username}
    </select>
    <select id="pageQuery" resultType="com.example.blog.entity.User">
        select * from user
        <if test="pageNo != null and pageNo > 0">
            limit ${(pageNo-1)*pageSize},#{pageSize}
        </if>
    </select>
    <select id="queryTotalNum" resultType="Integer">
        select count(*) from user
    </select>
    <insert id="insert">
        insert into user (nickname,username,password,email,create_time,update_time)
         values (#{nickname},#{username},#{password},#{email},#{createTime},#{updateTime})
    </insert>
</mapper>